# Data Model

> **Clean rebuild.** This is a v0.1 with no data to preserve. The schema is created by **one fresh
> initial Alembic migration** (`down_revision=None`). There is no backfill, no deprecated columns, and no
> compatibility aliases. The prior `data_sources` / `dataset_tables` model is fully replaced.

## Storage Technology

**SQLite** via SQLAlchemy 2.0 (sync) stores all **application metadata** (MCP servers + their
capability rows, sessions, query history). Alembic manages schema migrations. For an **internal**
(`parquet`) server the **data itself** lives in **Parquet** files on disk and is queried by DuckDB — it
is never copied into SQLite. For an **external** (`postgresql`, BETA) server the data stays in the remote
database; only connection metadata and an introspected table catalog are persisted.

A server's **tools/resources/prompts ARE stored as rows** (unlike the prior model). They are
LLM-generated by the sync pipeline and must be individually addressable, versioned, soft-deletable, and
cursor-paginated — so they are typed child tables, not a JSON blob. The dataset's generated **JSONSchema**
and its **physical-table catalog** are pure metadata → JSON columns on the server row.

### Agent memory store (separate, not Alembic-managed)

Durable per-session memory is stored by LangGraph's `SqliteSaver` in its **own SQLite file** (path from
`DATAANALYSIS_CHECKPOINT_DB`), keyed by `thread_id = session_id`. Its tables are owned by
`langgraph-checkpoint-sqlite` — **not** part of the SQLAlchemy models or Alembic migrations.

## Entities

### Entity: McpServer (`mcp_servers` / `McpServerRow`)

The primary entity — a dataset wrapped 1:1 by an MCP server. `name` is the dataset/server name (also the
agent "tool" name); `type` is the dataset type; `uri` is the dataset address (unique → 1:1). All other
dataset info lives in metadata columns.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | TEXT (UUID) | yes | Primary key |
| name | TEXT | yes | Server/dataset name = agent tool name. **UNIQUE** |
| title | TEXT | no | LLM-generated display title (sync stage a) |
| description | TEXT | no | LLM-generated server description (sync stage a); used as the MCP server description + the agent tool description |
| type | TEXT | yes | `parquet` (internal) or `postgresql` (external, BETA) |
| uri | TEXT | yes | Dataset URI. Internal: `parquet:///{name}`. External: `postgresql://user:pass@host:port/db` (credentials stored **plaintext** for v0.1 BETA; never logged/displayed — always via `DatasetURI.display()`). **UNIQUE** (1:1 dataset↔server) |
| version | INTEGER | yes | Monotonic capability version; default 1, bumped on each sync |
| dataset_schema_json | TEXT | no | Generated dataset JSONSchema `{tables:{…}, relationships:[…]}` (sync stage b) |
| physical_tables_json | TEXT | no | Physical-table catalog: `[{table, parquet_path, source_filename, columns, schema, row_count}]` (`parquet_path` NULL for external) |
| last_synced_at | TIMESTAMP | no | When the sync pipeline last ran |
| last_sync_status | TEXT | no | `ok` / `partial` / `error` (for the UI) |
| connection_error | TEXT | no | Last sanitized connection-check error (never credentials); null when healthy |
| created_at | TIMESTAMP | yes | When the server was created |

> Physical Parquet files live at `{datasets_dir}/{slug(name)}/{table}.parquet`. The URI carries no
> filesystem path; the absolute `parquet_path` of each table is recorded in `physical_tables_json`.

### The capability children — shared envelope

Each of `mcp_tools`, `mcp_resources`, `mcp_prompts` carries the same versioning + soft-delete envelope:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | TEXT (UUID) | yes | Primary key |
| server_id | TEXT (FK → mcp_servers.id) | yes | Owning server (indexed) |
| created_version | INTEGER | yes | `server.version` when this row was created |
| deleted_at | TIMESTAMP | no | Soft-delete tombstone — **NEVER hard-deleted**; null = active |
| deleted_version | INTEGER | no | `server.version` at soft-delete |
| created_at | TIMESTAMP | yes | Row creation time |
| updated_at | TIMESTAMP | yes | Last update time |

**Active view** (used everywhere a capability is listed/called): `server_id = ? AND deleted_at IS NULL
ORDER BY created_at, id`. Centralized in a repository helper so no endpoint forgets the filter.

### Entity: McpTool (`mcp_tools` / `McpToolRow`)  — a canned "GET-API" SELECT (sync stage d)

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| name | TEXT | yes | MCP tool `name` (machine id). **Partial-unique** `(server_id, name) WHERE deleted_at IS NULL` |
| title | TEXT | no | MCP tool `title` |
| description | TEXT | yes | MCP tool `description` |
| input_schema_json | TEXT | yes | MCP `inputSchema` (object JSONSchema for the canned query's parameters) |
| output_schema_json | TEXT | no | Optional MCP `outputSchema` |
| annotations_json | TEXT | no | Optional MCP `annotations` |
| sql_template | TEXT | yes | The underlying **parameterized** SELECT; params bound via DuckDB binding (never interpolation) |

### Entity: McpResource (`mcp_resources` / `McpResourceRow`)  — schema + entities (sync stages b/c)

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| uri | TEXT | yes | MCP resource URI (e.g. `dataset://{name}/schema`, `entity://{name}/{entity}`). **Partial-unique** `(server_id, uri) WHERE deleted_at IS NULL` |
| name | TEXT | yes | Resource name |
| title | TEXT | no | Resource title |
| description | TEXT | no | Resource description |
| mime_type | TEXT | no | e.g. `application/json`, `text/markdown` |
| kind | TEXT | yes | `schema` \| `primary_entity` \| `secondary_entity` |
| content_json | TEXT | no | Resource body (for `kind='schema'` this mirrors `dataset_schema_json`) |

### Entity: McpPrompt (`mcp_prompts` / `McpPromptRow`)  — prompt templates (sync stage e)

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| name | TEXT | yes | MCP prompt `name`. **Partial-unique** `(server_id, name) WHERE deleted_at IS NULL` |
| title | TEXT | no | Prompt title |
| description | TEXT | no | Prompt description |
| arguments_json | TEXT | no | MCP prompt `arguments`: `[{name, description, required}]` |
| template_json | TEXT | no | Messages template materialized by `prompts/get` |

### Entity: Session (`sessions` / `SessionRow`)

A named conversation over one or more MCP servers. The agent attaches one in-process pool server per
attached MCP-server entity. Joins **within** a server's tables are supported; cross-server joins happen
across ReAct iterations.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | TEXT (UUID) | yes | Primary key |
| name | TEXT | no | Optional; defaults to "Session YYYY-MM-DD HH:MM" |
| created_at | TIMESTAMP | yes | Session start time |
| updated_at | TIMESTAMP | yes | Last activity time |

### Entity: SessionMcpServer (`session_mcp_servers` join table)

Links Sessions to MCP servers (many-to-many).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| session_id | TEXT (FK → sessions.id) | yes | Composite PK |
| mcp_server_id | TEXT (FK → mcp_servers.id) | yes | Composite PK |

### Entity: QueryRecord (`query_records`)

One natural language question within a Session.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | TEXT (UUID) | yes | Primary key |
| session_id | TEXT (FK → Session.id) | yes | Owning session |
| question | TEXT | yes | The user's NL question |
| answer | TEXT | no | Final plain-text answer (null while processing) |
| status | TEXT | yes | `pending` / `completed` / `failed` |
| error_message | TEXT | no | Error detail if failed |
| iteration_count | INTEGER | no | Number of tool calls to reach the answer |
| query_history_json | TEXT | no | JSON array of `{"tool", "arguments", "result", "is_error", "capability"?}` — the tool-call trace (`tool` = server; optional `capability` only on generated GET-API tool calls) |
| input_tokens / output_tokens / total_tokens | INTEGER | no | Token usage across LLM calls |
| estimated_cost_usd | REAL | no | Estimated API cost |
| api_request_count | INTEGER | no | Number of LLM API calls |
| created_at / updated_at | TIMESTAMP | yes | Timestamps |

### Entity: AgentRun (`agent_runs`)

Internal record tracking each LangGraph pipeline invocation (one per QueryRecord).

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | TEXT (UUID) | yes | Primary key |
| query_record_id | TEXT (FK → QueryRecord.id) | yes | The query this run serves |
| status | TEXT | yes | `pending` / `completed` / `failed` |
| error_message | TEXT | no | Error detail if failed |
| created_at / updated_at | TIMESTAMP | yes | Timestamps |

## Relationships

```
McpServer (M) ──< SessionMcpServer >── (N) Session
McpServer (1) ──< McpTool (N)        (active = deleted_at IS NULL)
McpServer (1) ──< McpResource (N)    (active = deleted_at IS NULL)
McpServer (1) ──< McpPrompt (N)      (active = deleted_at IS NULL)
Session   (1) ──< QueryRecord (N)
QueryRecord (1) ──< AgentRun (N)
```

## Bootstrap: Upload → Create → Sync

1. **Upload** (`POST /mcpserver/upload`, auxiliary): `dataset_name` + CSV → Parquet at
   `{datasets_dir}/{slug(name)}/{table}.parquet`; returns the data URI `parquet:///{name}`. No entity.
2. **Create** (`POST /mcpserver`): URI + type → reject duplicate URI/name (1:1) → read the dataset dir →
   `physical_tables_json` → **`connection_check()` before commit** → insert `McpServer` (version 1) →
   **`run_sync()`** populates tools/resources/prompts. A broken server is never persisted.
3. **Sync** (`POST /mcpserver/{id}/sync`): regenerate capabilities incrementally; **transactional apply**
   (insert/update/soft-delete) and `version++`.

## Versioning & Soft-Delete (non-negotiable)

- `McpServer.version` is a monotonic counter bumped once per sync apply.
- Capability rows are **never hard-deleted** by sync or by the dispatcher. A removal sets `deleted_at` +
  `deleted_version`. Re-adding a previously-removed name inserts a new active row (the partial-unique
  index is over active rows only, so it doesn't collide with the tombstone).
- The whole sync apply runs in **one transaction** (request session commit/rollback): a partial sync
  never persists a bumped version with half-updated children.

## Data Lifecycle

- **McpServer**: created on `POST /mcpserver`; its capabilities regenerated by sync; deleted via explicit
  user action — `shutil.rmtree` of the dataset directory + the server row (cascade to capability rows,
  `SessionMcpServer` links, and QueryRecord/AgentRun via the session).
- **Capability rows**: created/updated/soft-deleted by sync; physically removed only when their server is
  deleted.
- **MCP pool**: per session — built lazily on the first query (`SessionPoolManager`), reused, idle/LRU
  evicted, closed on session delete + app shutdown. Rebuilt from each server's `physical_tables_json` /
  `uri`. `close(session_id)` is lock-safe.
- **Session / QueryRecord / AgentRun**: standard create-on-use, delete-with-parent.

## Migration note

**Fresh, single initial migration** (`down_revision=None`, batch mode for SQLite): creates `mcp_servers`,
`mcp_tools`, `mcp_resources`, `mcp_prompts`, `sessions`, `session_mcp_servers`, `query_records`,
`agent_runs`, with `server_id` indexes and the three **partial-unique** indexes (`WHERE deleted_at IS
NULL`). No backfill. `downgrade` drops everything. `alembic upgrade head` from empty builds the schema;
`alembic downgrade base` → `upgrade head` round-trips.

## Sensitive Data

- Uploaded files may contain PII; Parquet files are stored as-is on local disk (no special handling in
  v0.1). Multi-user deployments must add access controls.
- No LLM API keys stored in the database.
- External (`postgresql`, BETA) credentials are stored **plaintext** in `mcp_servers.uri` for v0.1 — never
  logged or displayed (every rendering goes through `DatasetURI.display()`, which strips credentials). A
  read-only `ATTACH` lets the agent read any table the credentials can reach, so the DB user must be
  scoped. External datasets are gated by `DATAANALYSIS_ENABLE_EXTERNAL_DATASETS` (default off; create
  returns 501 when off).
